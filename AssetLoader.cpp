
//asset loader loads premade objects into the frame
#include "AssetLoader.h"

cube* cubeloader(void)
{
    //creates a pointer to a new cube
    cube* newcube = (cube*) malloc(sizeof(cube));
    
    return newcube;
}

void sphereloader(void){
    
}
